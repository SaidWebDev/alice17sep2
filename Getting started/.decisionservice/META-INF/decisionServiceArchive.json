{
  "contentVersion" : "1.0.0",
  "buildTime" : "${buildTime}",
  "decisionServiceName" : "${project.parent.name}",
  "decisionServiceId" : "${decisionServiceId}",
  "decisionServiceVersion" : "${project.parent.version}",
  "engineRuntimeVersion" : "${engine-compact-runtime.version}",
  "engineAPIVersion" : "${engine-api.version}",
  "decisionId" : "${decisionId}",
  "commitId" : "${commitId}",
  "mlIntegrationRuntimeVersion" : "${ml-integration-runtime.version}",
  "decisionRepositoryId" : "${decisionRepositoryId}",
  "decisionRepositoryName" : "${decisionRepositoryName}",
  "decisionRepositoryURL" : "${decisionRepositoryURL}"
}